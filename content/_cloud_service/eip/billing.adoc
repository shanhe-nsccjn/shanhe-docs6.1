//EIP计费说明

本文介绍弹性公网 EIP 的收费详情。

== 收费模式

公网 IP 支持**按带宽计费**和**按流量计费**两种收费模式。两种模式均属为按需后付费方式，无需提前支付费用。两种收费模式可以互相转换。

include::../../_components/attention.adoc[]
收费模式不可频繁修改，每天只能修改 2 次。
include::../../_components/table_footer.adoc[]

[cols="1,3"]
|===
| 收费模式 | 适用场景

| <<_按带宽计费>>
| 适用于业务流量峰值在不同时间段比较平稳的场景。

| <<_按流量计费>>
| 适用于业务流量峰值在不同时间段波动较大的场景，如游戏、视频等行业。
|===

== 费用说明

=== 按带宽计费

按带宽计费根据您指定的带宽峰值进行费用计算，不同带宽对应不同的每小时使用单价。带宽大小可以在线实时进行调整，满足您对带宽的动态需求。

include::../../_components/note.adoc[]
出口带宽的价格会因为可用区的网络环境差异而不同。
include::../../_components/table_footer.adoc[]

按带宽计费的详细说明如下表。

[cols="1,3"]
|===
| 特性 | 说明

| 计费规则
| 根据指定的带宽峰值和计费时长收费，可随时释放。

|计费项
a| * 带宽费：按照您指定的带宽大小及实际使用时长计费，不限流量。
* IP 地址附加费：针对按带宽计费的 EIP，每个主账户名下都有免费的 IP 地址额度（默认为 2 个），当申请的 IP 地址个数超过免费额度时，系统会对额度外的每个 IP 收取固定的附加费用。该费用记在主账户名下。

|计费公式
a| EIP 实例费用 = 带宽费 + IP 地址附加费

其中： 

* 带宽费 = 固定带宽单价 * 计费时长
* IP 地址附加费 = IP 地址单价 * 计费时长


ifdef::pub[]
link:https://www.qingcloud.com/pricing#/EIP[点击此处]获取产品价格详情。
endif::[]

|===


=== 按流量计费

使用按流量计费，您需要为占用的公网 IP 地址付费，还需要为实际发生的公网流量付费。详细说明如下表。

[cols="1,3"]
|===
| 特性 | 说明

| 计费规则
| 根据每小时公网的实际流量计费，可随时释放。

|计费项
a| * IP 地址费：按 EIP 使用时间收取 IP 地址使用费。
* 流量费：按计费周期内实际发生的公网流量计费。流量数据每小时自动统计一次，分别对统计周期内的进、出流量求和，取其中较大的值作为扣费依据。其中，进流量费用只对超出 10Mbps 之后的流量计费。

|计费公式
a|EIP 实例费用 = IP 地址费 + 总流量费

其中： +

* IP 地址费 = IP 地址单价 * 计费时长
* 流量费 = Max(计费周期内进总流量, 计费周期内出总流量) * 流量单价

ifdef::pub[]
* 公网 IPv6 地址目前仅收取流量费，不收取 IP 地址费。
* link:https://www.qingcloud.com/pricing#/EIP[点击此处]获取产品价格详情。
endif::[]

|===

== 计费示例

带宽、流量以及 IP 地址的定价，与您所在的地域有关。以下示例中，不代入详细价格，仅展示计算过程。


*示例一：按带宽计费*

用户在某日 10:00:00 创建了一个使用按带宽收费模式的 EIP，带宽上限为 10Mbps，当日 15:00:00 时，将带宽调整为 20Mbps，则截止当日 20:00:00，该 EIP 使用产生的费用为：

EIP 费用 = 10Mbps带宽的单价 * 5（小时） + 20Mbps带宽的单价 * 5（小时）

*示例二：按流量计费*

用户创建了一个按流量计费的、带宽上限是 20Mbps 的 EIP，在一个扣费周期内有 30 分钟其下行速率一直保持在 18Mbps，有 10 分钟其上行速率一直保持在 10Mbps，则在这一个扣费周期中：

EIP 费用 = IP 使用费单价 * 1（小时） + 进/出总流量较大值 * 流量费单价

此处为便于计算，假设上下行速率一直稳定在一个固定值（实际计算时使用的是间隔为 1 分钟的采样速率），则进、出总流量的求和结果如下：

* 进流量之和 ＝ 8Mbps（18Mbps 减去优惠的 10Mbps） * 1800（秒）/8 = 1800 MB
* 出流量之和 ＝ 10Mbps * 600（秒数）/8 = 750 MB

因进流量之和大于出流量之和，所以该计费周期内的流量费 =  1800MB/1024 * 流量单价（元/GB）。


== 变配费用

EIP 支持变更收费模式和带宽上限，变更即时生效，变更后，将按照新的配置计费。您可以通过 EIP 详情页的**租赁信息**查看扣费详情。 

[cols="1,1,1,2"]
|===
|原收费模式|变更场景|生效时间|计费影响

.2+|按带宽计费
|修改收费模式
|立即生效
|按照新的配置开始计费。

|调整带宽上限
|立即生效
|按照新的配置开始计费。

.2+|按流量计费
|修改收费模式
|立即生效
|按照新的配置开始计费。

|调整带宽上限
|立即生效
|费用按流量结算，调整带宽不影响费用结算。

|===

== 资源欠费

如果用户余额已不足，EIP 资源会被自动暂停。

// tag::owe[]
* 对于通过云平台备案信息验证的 EIP，欠费之后有 10 天的资源保留期，并且平台会发送欠费提醒通知。保留期内，用户可随时充值来恢复资源。超过保留期仍未充值，资源将被删除，删除的资源会在回收站保留 2 小时，之后便会被彻底删除，无法再恢复。
* 对于不需要备案或备案信息没有通过验证的 EIP， 一旦欠费就会被系统释放回资源池。
// end::owe[]

为避免资源删除对您造成损失，请您留意账户余额，并及时充值。
